services:
  homelab-landing:
    build: .
    container_name: homelab-landing
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production

# If you want to connect to other containers, uncomment and configure networks
# networks:
#   - homelab
